# Example GRUB configuration for NVIDIA
# Location: /etc/default/grub
#
# After modifying, run:
# sudo update-grub

# Default entry
GRUB_DEFAULT=0

# Timeout
GRUB_TIMEOUT=5
GRUB_TIMEOUT_STYLE=menu

# Kernel parameters for NVIDIA
# nvidia-drm.modeset=1 — REQUIRED for Wayland (enables DRM kernel modesetting)
# nvidia-drm.fbdev=1   — Recommended on kernel 6.x+ (framebuffer console)
# nvidia.NVreg_PreserveVideoMemoryAllocations=1 — Required for suspend/resume on Wayland
GRUB_CMDLINE_LINUX_DEFAULT="quiet splash nvidia-drm.modeset=1 nvidia-drm.fbdev=1 nvidia.NVreg_PreserveVideoMemoryAllocations=1"

# For ACPI issues (common on Parrot OS 7):
# GRUB_CMDLINE_LINUX_DEFAULT="quiet splash nvidia-drm.modeset=1 noapic"

# For severe ACPI problems (last resort, disables power management):
# GRUB_CMDLINE_LINUX_DEFAULT="quiet splash nvidia-drm.modeset=1 acpi=off"

# For debugging boot issues:
# GRUB_CMDLINE_LINUX_DEFAULT="nvidia-drm.modeset=1 nomodeset debug"

# Graphics card detection
GRUB_GFXMODE=auto

# Hidden menu (comment out to show menu)
# GRUB_TIMEOUT_STYLE=hidden

# Distribution specific
GRUB_DISTRIBUTOR=`lsb_release -i -s 2> /dev/null || echo Debian`

# Recovery mode
GRUB_DISABLE_RECOVERY="false"
